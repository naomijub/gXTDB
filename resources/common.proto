syntax = "proto3";

package com.xtdb.protos;

message Empty {}

message OptionString {
    oneof value {
      Empty none = 1;
      string some = 2;
    }
  }

message OptionDatetime {
  oneof value {
    Empty none = 1;
    string some = 2;
  }
}